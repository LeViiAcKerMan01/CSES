#include<bits/stdc++.h>
using namespace std;

int main(){
    map<int, int> mp;
    int n;
    cin >> n;
    vector<int> arr(n, 0);
    for(int i = 0; i < n; i++){
        cin >> arr[i];
        mp[arr[i]]++;
        
    }
}

